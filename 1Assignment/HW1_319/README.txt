Right click on ChatServer and select run as -> java application once.

Right click on ChatClient and select run as -> java application as many times as necessary to represent people using an instance of ChatClient.

On ChatClient console enter a name and the client connects to the server, then enter a message and \n and message will be sent to all other connected users.